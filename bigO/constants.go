package bigO

//function that recieve an array of integers
//to considere a thing like a constant, this one hasn't to be involved directly
//with the algoritm process (doesn't scale any input) O(1)
func constant(array []int) int {
	var result = 5 + 5
	return result
}
